const TestDecimal = artifacts.require("./TestDecimal.sol")
const assert = require('assert')
const { assertThrow } = require('./test-util.js')

contract('TestDecimal', accounts => {
  it('should have tests', async function() {
    throw new Error()
  })
})
